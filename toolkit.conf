TOOLKIT_NAME="WLAN Pi Wireless Engineering Toolkit"
TOOLKIT_VERSION="1.0.0"

WET_INSTALL_ROOT="/home/wlanpi/wlanpi-toolkit"

DEFAULT_INTERFACE="wlan0"

SURVEY_SNAPSHOT_DIR="${WET_INSTALL_ROOT}/reports/.survey_snapshot"
SURVEY_EXPORT_DIR="${WET_INSTALL_ROOT}/reports/surveys"

CAPTURE_DIR="${WET_INSTALL_ROOT}/captures"

WET_HOST="${WET_HOST:-198.18.42.1}"
WET_USER="${WET_USER:-wlanpi}"
